# MLPClassifier
This project implements a MLP neural network classifier using the Breast Cancer Wisconsin dataset from Scikit-learn. The goal is to classify tumors as malignant or benign based on various medical features. Different hidden layer architectures are implemented and compared to observe how network depth and number of neurons affect model performance.
